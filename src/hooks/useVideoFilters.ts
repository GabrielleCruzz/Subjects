import { useState, useMemo } from 'react'
import { Video } from '@/types'

// This hook is no longer needed as the filtering logic is simple
// and has been moved directly into the Videos.tsx component.
// This file can be removed.
